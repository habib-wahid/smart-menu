spring:
  application:
    name: menu-app

  datasource:
    url: jdbc:postgresql://localhost:5432/smart_menu
    username: postgres
    password: root

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true