<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="
        http://www.liquibase.org/xml/ns/dbchangelog
        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.3.xsd">

    <changeSet id="create-order-audit-table" author="habib-wahid">
        <createTable tableName="customer_order_audit">
            <column name="id" type="bigint">
                <constraints nullable="false"/>
            </column>

            <column name="user_id" type="bigint">
                <constraints nullable="false"/>
            </column>

            <column name="order_status" type="varchar(255)">
                <constraints nullable="false"/>
            </column>

            <column name="total_price" type="double precision">
                <constraints nullable="false"/>
            </column>

            <column name="total_item_price" type="double precision">
                <constraints nullable="false"/>
            </column>

            <column name="total_addon_price" type="double precision"/>

            <column name="paid" type="boolean"/>
            <column name="served" type="boolean"/>
            <column name="table_no" type="int"/>
            <column name="order_time" type="timestamp"/>
            <column name="order_update_time" type="timestamp"/>
            <column name="order_delivery_time" type="timestamp"/>

            <!-- Envers metadata -->
            <column name="rev" type="int">
                <constraints nullable="false"/>
            </column>

            <column name="revtype" type="smallint">
                <constraints nullable="false"/>
            </column>
        </createTable>

        <!-- Create FK to revinfo (Envers requirement) -->
        <addForeignKeyConstraint
                baseTableName="customer_order_audit"
                baseColumnNames="rev"
                constraintName="fk_order_audit_rev"
                referencedTableName="revinfo"
                referencedColumnNames="rev"/>
    </changeSet>

</databaseChangeLog>